# 🏋️ Programa de Academia - Instruções para GitHub Copilot

## Visão Geral do Projeto
Sistema web de gerenciamento de treinos de academia com:
- **Usuários**: Sistema de login com múltiplos usuários
- **Admin**: Painel de administração para criar exercícios e atribuir treinos
- **Treino**: Interface para usuários completar seus exercícios
- **Histórico**: Rastreamento de treinos completados

## Stack Tecnológico
- **Frontend**: HTML5, CSS3, JavaScript Vanilla (sem frameworks)
- **Backend**: PHP 8.2.12 (opcional, localStorage por padrão)
- **Database**: MySQL (schema criado, mas usando localStorage)
- **Server**: XAMPP com Apache 2.4.58
- **Localização**: C:\xampp\htdocs\academia_treino\

## Estrutura de Arquivos Críticos

```
├── index.html           # Dashboard principal do usuário
├── login.html          # Autenticação de usuários
├── admin.html          # Painel de administrador (usuários, exercícios, treinos, histórico)
├── admin-login.html    # Autenticação do admin
├── css/
│   └── styles.css      # Estilos globais com temas modernos
├── js/
│   └── main.js         # Lógica principal (494+ linhas)
└── videos/             # Pasta com vídeos MP4 dos exercícios
```

## Estruturas de Dados Principais

### Usuário Logado (localStorage: 'currentUser')
```javascript
{
  id: 1,
  name: "Leandro Barba",
  username: "Leandro",
  routine: "PPL 1 NOVO",
  goal: "Redução de gordura/Hipertrofia - Intermediário"
}
```

### Exercício
```javascript
{
  id: 1,
  name: "Supino reto barra",
  grupo: "push|pull|legs",
  series: "3x12",
  load: "10kg",
  interval: "50s",
  instructions: "",
  completed: false
}
```

### Treino do Usuário (localStorage: 'training_[username]')
```javascript
{
  push: [1, 2, 3, 4, 5],      // IDs dos exercícios
  pull: [6, 7],
  legs: [8, 9]
}
```

### Personalização de Treino (localStorage: 'custom_training_[username]')
```javascript
{
  1: { series: "3x12", load: "10kg" },  // ID do exercício
  2: { series: "4x10", load: "12kg" }
}
```

### Sessão de Treino (localStorage: 'training_session_[username]')
```javascript
{
  date: "Tue Feb 04 2026",
  startTime: 1707000000000,  // timestamp
  elapsedSeconds: 1250        // segundos passados
}
```

## Funcionalidades Implementadas

✅ **Autenticação**
- Login com múltiplos usuários (demo users)
- Admin login separado
- Logout com limpeza de sessão

✅ **Dashboard do Usuário**
- Exibição de treino personalizado (Push/Pull/Legs)
- Contador de tempo de treino em tempo real
- Checkbox para marcar exercícios como concluído
- Edição de carga/peso
- Visualização de vídeos (busca dinâmica por nome)

✅ **Painel Admin**
- Gerenciamento de usuários (criar/editar)
- Gerenciamento de exercícios (criar/editar/listar)
- Atribuição de exercícios a usuários por grupo
- Edição de carga e séries personalizadas
- Histórico de treinos por usuário

✅ **Sistema de Timer**
- Começa automaticamente no login
- Persiste durante o dia (reseta meia-noite)
- Exibe em HH:MM:SS

✅ **Resumo de Treino**
- Modal ao completar todos os exercícios
- Mostra: quantidade de exercícios, tempo total, carga total
- Gráfico de dias de treino na semana
- Reseta treino para próximo dia

## Convenções de Código

### JavaScript
- Nomes em camelCase
- Funções prefixadas com verbos (load, render, save, toggle, etc)
- Use `const` por padrão, `let` para variáveis mutáveis
- Adicione comments para lógica complexa

### CSS
- Usar classes em kebab-case
- Grid/Flexbox para layouts
- Mobile-first quando possível
- Variáveis CSS para cores principais: #667eea (principal), #764ba2 (secundária)

### HTML
- Semântica HTML5
- IDs para elementos únicos
- Classes para estilos
- Atributos data-* para dados customizados

## Fluxos Principais

### Fluxo de Login
1. Usuário acessa login.html
2. Insere credenciais
3. Sistema valida contra DEMO_USERS
4. Salva 'currentUser' em localStorage
5. Redireciona para index.html
6. init() inicia checkLogin() → initializeDemoData() → loadTraining() → startTrainingTimer()

### Fluxo de Treino
1. Usuario marca exercício como completo
2. toggleComplete() salva em localStorage
3. checkIfWorkoutComplete() verifica se todos estão completos
4. Se sim, showWorkoutSummary() exibe modal de resumo
5. finishSummary() reseta treino e abre página para próximo dia

### Fluxo Admin
1. Admin acessa admin-login.html
2. Autentica (admin/admin123)
3. Acessa admin.html com 4 abas:
   - Usuários: CRUD de usuários
   - Exercícios: CRUD de exercícios (DEMO_EXERCISES)
   - Treinos: Atribuição de exercícios a usuários + personalização
   - Histórico: Visualização de treinos completados

## Variáveis Globais Importantes

```javascript
STORAGE_KEY = 'academia_treino'
USE_DATABASE = false              // true = usar API PHP, false = localStorage
CURRENT_USER = null               // Usuário logado
USUARIO_ID = 1                    // ID do usuário
sessionElapsedSeconds = 0         // Tempo do treino atual
allUsers = {...}                  // Usuários do admin
selectedUser = null               // Usuário selecionado no admin
```

## Performance & Otimizações

- Cache busting com ?v= em scripts e styles
- localStorage para persistência offline
- Lazy loading de vídeos
- Debounce em atualizações frequentes

## Problemas Conhecidos & Soluções

### Vídeos não aparecem
- Verificar se arquivos estão em `/videos/` com nomes exatos
- O algoritmo normaliza acentos: "Agachamento Livre.mp4" → "agachamento livre"

### localStorage lotado
- Limpar com DevTools → Application → Local Storage → Clear

### Timer não funciona
- Verificar se sessionStartTime está definido
- Confirmar se localStorage 'training_session_[username]' existe

## Próximas Melhorias Sugeridas

- [ ] Integrar com banco de dados real (PHP/MySQL)
- [ ] Gráficos de progressão de carga ao longo do tempo
- [ ] Sistema de notificações push
- [ ] App mobile com React Native
- [ ] Sincronização com wearables (Apple Watch, Fitbit)
- [ ] Análise de performance com AI
- [ ] Sistema de badges/achievements

## Comandos Úteis

```bash
# Copiar para XAMPP
Copy-Item -Path "C:\Users\Leandro\Desktop\Programa_cantina\*" -Destination "C:\xampp\htdocs\academia_treino\" -Force -Recurse

# Limpar localStorage (DevTools)
localStorage.clear()

# Ver dados de usuário
JSON.parse(localStorage.getItem('currentUser'))

# Ver treino de usuário
JSON.parse(localStorage.getItem('training_Leandro'))

# Ver histórico de sessão
JSON.parse(localStorage.getItem('training_session_Leandro'))
```

## Padrões de Requisição de Ajuda

Ao pedir ajuda ao Copilot, use:
- "Crie uma função que..." (para novos recursos)
- "Corrija o bug em..." (para correções)
- "Refatore a função X para..." (para melhorias)
- "Explique por que..." (para entendimento)
- "Adicione validação em..." (para segurança)

---

**Última Atualização**: 04 de Fevereiro de 2026
**Versão**: 1.0
**Status**: Em Desenvolvimento 🚀
